import React, { useState } from 'react';
import axios from 'axios';
export default function RegisterPage(){ const [name,setName]=useState(''); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const doReg=()=>{ axios.post((process.env.REACT_APP_API_URL||'http://localhost:5000/api')+'/auth/register',{ name, email, password }).then(r=>{ localStorage.setItem('token', r.data.token); localStorage.setItem('role', r.data.user.role); window.location='/dashboard'; }).catch(e=>alert('Register failed')); }; return (<div className='p-4'><h2>Register</h2><input placeholder='Name' value={name} onChange={e=>setName(e.target.value)} /><input placeholder='Email' value={email} onChange={e=>setEmail(e.target.value)} /><input placeholder='Password' type='password' value={password} onChange={e=>setPassword(e.target.value)} /><button onClick={doReg}>Register</button></div>); }
